# V1 验收执行记录

用于记录 V1 收尾阶段的执行状态，避免后续重复检查同一阶段。

## 阶段状态

- [x] 阶段 1：同步 Supabase/开发计划文档，明确媒体上传已完成与当前验收任务。
  - 完成时间：2026-06-05
  - 验证：`npm run typecheck`
  - 提交：`1b8b465 docs: sync v1 acceptance plan`
- [x] 阶段 2：执行桌面与移动端冒烟检查，必要时修复 UI/流程问题。
- [ ] 阶段 3：部署前最终检查与总结提交。

## 当前注意事项

- 工作区存在未跟踪的 `.claude/`，不是本轮任务产物，不纳入提交。
- `npm run lint` 当前只有 Next.js `<img>` 优化警告，无错误。

## 2026-06-06 冒烟检查记录

- 移动端首页：确认首屏为学习工作台，无页面级横向溢出；修复最近剧集“查看片段”触控高度低于 40px。
- 移动端学习工作台：修复字幕队列跟随播放触发整页滚动的问题，改为只滚动字幕队列容器；播放器首屏保持可见。
- 学习工作台响应式：保留上次播放器宽度修正，主列和侧栏加入 `min-w-0`，移动端播放器使用固定响应式高度，播放控制改为稳定三列。
- 桌面端首页/学习工作台：复查无非预期横向溢出，无低于 40px 的可见链接/按钮。
- 验证：`npm run lint`、`npm run typecheck`、`npm run build` 均通过；lint 仍只有既有 `<img>` 警告。
- 备注：`/smoke-learning-studio` 未登录时会触发一次媒体签名 URL 401 控制台日志，页面仍保留 mock 媒体路径用于 smoke。
